#include<iostream>

using namespace std;

int main()
/*
{
    int marks = 0;

    cout << "Enter your marks : ";
    cin >> marks;

    if(marks >= 60 && marks <= 100)
    {
        cout << endl << "You are pass!!!" << endl; 
    }
    else if(marks >= 0 && marks <= 60)
    {
        cout << endl << "you are fail!!" << endl ;
    }
    else
    {
        cout << endl << "Invalid Input!!" << endl;
    }
    return 0;
}
*/

/*WAP to check if a given number is even or odd
{
    int num;

    cout << "Enter a number: ";
    cin >> num;

    if(num%2==0)
    {
        cout << endl << "Number is even"<< endl;
    }
    else
    {
        cout << endl << "Number is odd" << endl;
    }
    return 0;
}
*/

/*WAP to check if a given number is multiple of 3 or not
{
    int num;

    cout << "Enter a number : ";
    cin >> num;

    if(num%3==0)
    {
        cout << "Number is multiple of 3" << endl;
    }
    else
    {
        cout << "Number is not multiple of 3" << endl;
    }
}
*/

/*WAP to check a number is in the range of 100 to 200 or not
{
    int num;

    cout << "Enter a number : ";
    cin >> num;

    if(num >= 100 && num <=200)
    {
        cout << "Number is in the range" << endl;
    }
    else
    {
        cout << "Number is not in the range" << endl;
    }
}
*/

/*WAP to find largest number among three given number
{
    int num1,num2,num3;

    cout << "Enter a three number :";
    cin >> num1 >> num2 >> num3;

    if(num1>num2 && num1>num3)
        cout << "num1 is a largest" << endl;
    else if(num2>num1 && num2>num3)
        cout << "num2 is largest" << endl;
    else
        cout << "num3 is largest" << endl;
    return 0;
}
*/

//WAP to check the entered character is vowel or not
{
    char ch;

    cout << "Enter the character : ";
    cin >> ch;

    if(ch=='a' || ch=='e' || ch=='i' || ch=='o' || ch=='u')
        cout << "char is vowel" << endl;
    else if(ch=='A' || ch=='E' || ch=='I'||  ch=='O' || ch=='U')
        cout << "char is vowel" << endl;
    else
        cout << "char is not an vowel" << endl;

}